# Deterministic UI fixtures

Fixtures in this directory are non-secret, versioned inputs shared by prototype and implementation screenshot scenarios. They must use fixed IDs/timestamps, deterministic catalog responses and explicit empty/loading/error variants. Never include real credentials, recordings or provider calls.
