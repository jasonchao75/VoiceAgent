# Screenshot evidence

These files are retained implementation snapshots from the earlier manual review. They are not approved prototype baselines and must not be used to update a baseline automatically.

- Captured: 2026-09-08
- Browser: local Google Chrome headless
- Viewport: 1440 × 1000
- Source: local temporary SQLite data with one non-secret Bot; no provider API calls
- Files: Bot settings, Bot editor, ASR/LLM/TTS drawers, and Sessions list

Chat/Web call live turns, barge-in, provider diagnostics, and recording playback require the
reviewer's BYOK credentials and are intentionally left for opt-in local acceptance.
